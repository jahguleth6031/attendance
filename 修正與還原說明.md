# 出勤系統修正版（2026-09-15）

## 備份

修改前完整程式碼已保存在 GitHub 分支 `backup-before-fixes-2026-09-15`，原始提交 `e5c509966a44be6f99edc8533e7576164d171e04`。
本機另有 `attendance-before-2026-09-15/index.html`。
這些是程式碼備份，不含雲端員工資料。使用新版前建議先從 App 匯出 JSON 資料備份。

## 已修正

- 加班費按半小時單價計算；兩小時、每半小時100元，結果400元。
- 勞保個人小計與分項一致；雇主職災只計一次。
- 刪除與移動員工時，同步調整所有舊索引；新增永久識別碼供新薪資紀錄使用。
- 新發薪紀錄保存每人當時明細；历史報表不再用現在的薪資重算。
- 舊歷史紀錄保留原先總額；沒有保存的個人實領明確標示缺少。
- 超休計算可退回之前多扣金額；同月後續期別存在時先阻擋回頭改寫。
- 重疊與跨月發薪區間會提示；未登錄的月薪出勤在發薪前要求確認。
- 讀取資料不再製造缺勤紀錄；上下午重疊或反向工時會阻擋。
- 使用伺服器讀取、即時監聽與交易版本檢查，防止同帳號的多裝置互相覆蓋。
- 載入失败時停止寫入；切換帳號清除舊狀態，忽略前次尚未完成的讀取。
- 匯入與薪資產生會等待實際雲端確認；有未儲存資料時提醒，並提供重試與重新讀取。
- 檢查 JSON 結構及數字，轉義顯示姓名／備註等文字。
- 四分之一 A4 格式保留，長文字可換行，列印前攔截明顯超出格子的內容。

## 使用上的差異

1. 發薪重算：若要修改先前期別，先刪除同月份較晚的期別，再從前到後重算。
2. 「已記錄實領總額」讀取發薪當時保存的金額，不包含雇主額外費用。
3. 多裝置衝突時，先匯出本機變更備份，再重新讀取雲端；不會自動覆蓋他人的版本。
4. 舊程式沒有保存的個人薪資明細無法追溯重建，需參考當時 PDF 或備份。

## 本次未完成的架構項目

- 不同 Google 帳號的公司共用資料及主管／代理人權限，需要另行遷移資料並配置 Firestore Rules。本次仍使用原有個人 UID 路徑。
- 正式 Firestore 規則、定期雲端備份、多文件儲存架構未修改。大資料量會提示容量問題，需另行拆分。
- 月薪分三期發放的不同本薪金額尚無新設定；本薪仍沿用現有欄位。
- 完整年度勞健保級距、健保雇主平均眷口及各類身分制度未在本次全面更新，請按正式繳費單核對。勞保修正是分項一致性與職災重複的修正。
- 保留現有瀏覽器列印；尚未完成真實 iOS 列印驗證，也未改成獨立 PDF 引擎。

## 驗證

23項 JavaScript 回歸測試通過；執行方式：在本目錄執行 `node regression-tests.cjs`。
另以獨立假資料在 Chrome 檢查載入、加班400元、日薪半天900元及薪資單實領位置。
測試沒有寫入正式 Firebase 員工資料。

## 還原

先匯出目前的 JSON 資料，再將備份分支的 index.html 覆蓋 main 的 index.html。
注意：舊程式不認識新版保存的個人薪資明細與 employeeIds，之後用舊程式儲存可能捨棄這些新增欄位，所以退版前務必保存 JSON。
